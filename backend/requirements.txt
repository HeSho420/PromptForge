flask>=3.0
pillow>=10.0
anthropic>=0.90  # API LLM fallback only; the app runs without credentials
# Reads and writes real video. Ships a static ffmpeg binary inside the wheel,
# so there is nothing to install system-wide and nothing on PATH to depend on
# (this machine had neither ffmpeg nor any decoder). Needed to turn an
# uploaded driving video into frames, and frames back into a clip.
imageio-ffmpeg>=0.5
# iPhones hand over HEIC by default; registers a PIL opener so the ingest
# normalizer (storage._normalize_image) can decode it - converted to PNG once
# at the door, so browsers, ComfyUI and thumbnails never meet the container.
pillow-heif>=1.0
